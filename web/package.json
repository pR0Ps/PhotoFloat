{
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf build/",
    "build:js":
      "mkdir -p build/js && uglifyjs js/*.js --compress --mangle -o build/js/scripts.min.js --source-map \"url='scripts.min.js.map',includeSources=true\" ",
    "build:css":
      "mkdir -p build/css && csso css/* -o build/css/styles.min.css --map build/css/styles.min.css.map",
    "build": "npm run clean && npm run build:js && npm run build:css && cp -R public/* build/"
  },
  "license": "GPL-2.0",
  "devDependencies": {
    "csso-cli": "^1.0.0",
    "prettier": "^1.6.1",
    "uglify-js": "^3.0.28"
  }
}
